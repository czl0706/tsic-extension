最重要的守則：除了叫做 description.md 以外的檔案都不准寫入（除非使用者當下明確指示要改某個檔案）

目標：讓每個 description.md 都有英語、中文兩版，清晰好懂

背景：
- 這是一個教學用途的 VScode extension，所以 description 除了 SPEC 之外還有教學用途。
- 但語氣要分區：**前段是正式 SPEC，後段才是教學**。不要整篇都像教科書。

實作規範：

一、整體排版
- 英文先、中文後，同一份文件，中間用分隔線 `---` 隔開。
- 英文段與中文段的架構要一致（同樣的分段、同樣的表格）。

二、文件架構（英文段與中文段各一份，分段如下）

# 摘要 / Summary
- 幾句話講清楚：這個模組做什麼、輸入輸出的概念、有沒有狀態/時脈。語氣精簡、像規格開頭。

# 規格 / Specification（正式 SPEC 語氣，放前面）
## 概觀 / Overview
- 類型（組合 / 循序）、時序（有無時脈延遲）、輸入與輸出的概述。
## 埠列表 / Port List
- 表格欄位：名稱 ｜ 方向 ｜ 寬度 ｜ 型別（wire/reg）｜ 說明。
## 功能描述 / Functional Description
- 表格逐一列出每個輸出的「定義式」與「結果值／行為」，要精確、像 datasheet。

# 重點語法與概念 / Key Syntax & Concepts（教學語氣，放後面）
## 1. 概念名稱
- 一小段 code（```verilog```）＋ 扼要解說。解說要精簡，不要過度展開、不要冗長條列。
## 2. …
## 補充 / Notes
- 補充提醒、常見誤解、對照說明等。

三、mermaid 使用原則（選用，不是必備）
- 只有在「圖比文字或表格更清楚」時才畫；**不要為了用而用**。
- 適合的情況：FSM 狀態轉移圖、資料路徑（datapath）/管線結構、多模組互接關係、演算法流程。
- 不適合：單模組純扇出、用埠列表/功能描述表就講得清楚的東西——這種一律不放圖。
- 語法注意：node 標籤含 `[` `]` `{` `}` `'` `@` 等字元時，用雙引號把標籤包起來，例如 `A["in [7:0]"]`。

注意事項：
- 不要提到任何跟 Testbench 技巧、知識有關的內容，這不在這個擴充的教學範圍內。

中國用語警報：
避免中國用語，應改為台灣用語。已確立的對照（持續補充）：
- 端口 → 埠
- 位 / 比特 → 位元
- 信號 → 訊號
- 默認 → 預設
- 符號擴展 → 符號延伸
- 拼接 → 串接
- 綜合 → 合成
- 觸發器 → 正反器（flip-flop）
- 總線 → 匯流排
